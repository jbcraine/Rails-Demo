using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//Bring the Player to a new area (Go through a door, change scene. Anything that goes beyond the standark Pint & Click control.)
public class AreaProceeder : Interactable
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
